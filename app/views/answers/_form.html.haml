/ .container
/   - if @errors
/     - @errors.each do |error|
/       %p= error
/   .col-lg-12
%h3 Submit an answer!
= form_for([question, answer]) do |f|
  =f.text_field :content
  =f.submit value: 'Submit Answer'
-if flash[:answer_errors]
  %ul
    - flash[:answer_errors].each do |error|
      %li=error

